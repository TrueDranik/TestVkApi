spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:comments-collector
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
